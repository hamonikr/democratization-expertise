<%@ page language="java" contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"%>
<c:if test="${selectedCnt >= 3}">
<img src="/img/icon_replysel.png" alt="답변채택" width="25px" height="25px">
</c:if>
<c:if test="${selectedCnt >= 5}">
<img src="/img/icon_replysel.png" alt="답변채택" width="25px" height="25px">
</c:if>
<c:if test="${selectedCnt >= 10}">
<img src="/img/icon_replysel.png" alt="답변채택" width="25px" height="25px">
</c:if>
<c:if test="${selectedCnt >= 20}">
<img src="/img/icon_replysel.png" alt="답변채택" width="25px" height="25px">
</c:if>
<c:if test="${selectedCnt >= 30}">
<img src="/img/icon_replysel.png" alt="답변채택" width="25px" height="25px">
</c:if>
<c:if test="${selectedCnt >= 60}">
<img src="/img/icon_replysel.png" alt="답변채택" width="25px" height="25px">
</c:if>
<c:if test="${selectedCnt >= 100}">
<img src="/img/icon_replysel.png" alt="답변채택" width="25px" height="25px">
</c:if>

